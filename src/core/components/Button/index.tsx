import React from 'react';
import './styles.css';

const Button = () => (
    <button 
        type="button"
        className="button"
    >
        Encontrar
    </button>
);

export default Button;